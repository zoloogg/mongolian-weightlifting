import { ClockController } from '../../types/controllers'

export const clockController: ClockController = {
  get: async (req, res) => {
    res.json({})
  },
  set: async (req, res) => {
    res.json({})
  },
  pause: async (req, res) => {
    res.json({})
  },
  clear: async (req, res) => {
    res.json({})
  },
}
